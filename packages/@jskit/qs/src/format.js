/**
 * URL格式化
 *
 * @param { string } url 数据
 * @returns { string } 返回新的 url
 */
function formatUrl(url) {
  if (!url) return;

  let newUrl = '';

  newUrl = url;

  return newUrl;
}

export default formatUrl;
